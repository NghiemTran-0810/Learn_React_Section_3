import React from "react";
class Foo extends React.Component {
    // JSX
    render() {
        return (
            <p>my first component in ReactJS with random number {Math.random()}</p>
        )
    }
}

export default Foo;